# AIGC Web

React + TypeScript + Vite starter project.

## Requirements

- Node.js 20.18 or newer
- npm 10 or newer

## Scripts

```bash
npm install
npm run dev
npm run lint
npm run build
npm run preview
```

## Development

The app entry is `src/main.tsx`, and the main component is `src/App.tsx`.
